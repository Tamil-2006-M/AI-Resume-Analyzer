"""
database package
=================
Holds db.py (Phase 7), which opens the MySQL connection and runs the
INSERT / SELECT queries. Keeping database code in its own file means
app.py never contains raw SQL.
"""
